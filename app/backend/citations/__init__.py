"""Formatted-citation rendering (inc 106) — the word-processor-integration spine.

Renders a paper's canonical CSL-JSON into formatted in-text citations + bibliography entries in real CSL
styles via the bundled citeproc-js sidecar. Local, no network, no LLM, no egress.
"""
