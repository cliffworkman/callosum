"""Literature discovery (backlog #28): a SourceProvider registry + Search over external metadata sources.

The complete result list is always returned (AI augments, never filters — that is SP1b's axis-relevance highlight,
a hint not a gate). Save is metadata-only (no PDF fetch). Public-metadata search (Crossref now; PubMed/bioRxiv
later) — NOT the Gemini egress gate.
"""
