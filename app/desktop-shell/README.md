# Desktop Shell

Planned (post-V1): a Tauri desktop wrapper around the local app.

Not yet implemented. The current application runs as a local FastAPI server and browser UI.

Tracked under **"Packaging & distribution (post-V1)"** (Theme 4) in
`.claude/docs/INCREMENT-BACKLOG.md` (with OS-keychain secret storage and desktop distribution).
