{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the splash and main windows. The splash page only needs to listen for backend-status events and invoke retry_backend; the main window is the real callosum UI and never calls into Tauri.",
  "windows": ["splash", "main"],
  "permissions": ["core:default"]
}
