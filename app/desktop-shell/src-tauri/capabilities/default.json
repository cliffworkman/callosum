{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the splash and main windows. The splash page only needs to listen for backend-status events and invoke retry_backend; the main window is the real callosum UI, which also listens for update-ready events and invokes install_update_now/open_release_page (the auto-updater, inc 'desktop-shell auto-updater').",
  "windows": ["splash", "main"],
  "remote": {
    "urls": ["http://127.0.0.1:*/*"]
  },
  "permissions": [
    "core:default",
    "allow-retry-backend",
    "allow-install-update-now",
    "allow-open-release-page",
    "allow-check-for-updates-now",
    "allow-quick-tunnel-status",
    "allow-start-quick-tunnel",
    "allow-stop-quick-tunnel"
  ]
}
