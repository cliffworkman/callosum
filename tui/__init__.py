"""callosum TUI — a terminal client covering the full feature surface of the running app.

Run `python -m tui` for the numbered-menu REPL, or `python -m tui <group> <action>` one-shot
(the agent path; add `--agent --format json`). See tui/README.md.
"""
