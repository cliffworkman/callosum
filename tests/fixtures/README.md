# Test Fixtures

Placeholder directory.

Current tests use inline fixtures, temporary files, fake clients/models, and shared setup in `tests/conftest.py`. No committed fixture corpus is required today.
